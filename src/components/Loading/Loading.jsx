import React, { useState, useEffect } from "react";
import "./Loading.css"; // Archivo de estilos para la pantalla de carga

export const LoadingScreen = () => {
  return (
    <div className="loading-screen">
      <div className="loader">test</div>
    </div>
  );
};
